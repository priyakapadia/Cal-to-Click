package jayangel.camtest2;

/**
 * Created by jaybob320 on 12/6/15.
 */
public class SetCalendarEvent {


/*
    public void addToCalendar() {

        Intent calIntent = new Intent(Intent.ACTION_INSERT);
        calIntent.setData(CalendarContract.Events.CONTENT_URI);
       // calIntent.putExtra(Events.TITLE, getCalTitle());
        //calIntent.putExtra(Events.EVENT_LOCATION, getCalLocation());
        //calIntent.putExtra(Events.DESCRIPTION, getCalDescription());
        //GregorianCalendar calDate = new GregorianCalendar(getCalYear(), getCalDay(), getCalMonth());
        //calIntent.putExtra(CalendarContract.EXTRA_EVENT_ALL_DAY, isAllDayEvent());
        //calIntent.putExtra(CalendarContract.EXTRA_EVENT_BEGIN_TIME, calDate.getTimeInMillis());
        //calIntent.putExtra(CalendarContract.EXTRA_EVENT_END_TIME, calDate.getTimeInMillis());
//calIntent.setData(CalendarContract.Events.CONTENT_URI);
        //calIntent.putExtra(Events.AVAILABILITY, Events.AVAILABILITY_BUSY);
        calIntent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK);

        startActivity(calIntent); // *************** ERROR HERE ***************** On startActivity
    }
    */



}
